const fs = require('fs');

function analyzeCode(path) {
    // Logique d'analyse du code ici
    console.log(`Analyse du code à l'emplacement : ${path}`);
    // Simuler une analyse
dependencyList = ['package1', 'package2'];
    return dependencyList;
}

module.exports = analyzeCode;